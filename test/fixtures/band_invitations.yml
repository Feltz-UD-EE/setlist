# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

one:
  band: one
  token_digest: invitation_digest_one
  expires_at: <%= 1.week.from_now %>

two:
  band: two
  token_digest: invitation_digest_two
  expires_at: <%= 1.week.from_now %>
